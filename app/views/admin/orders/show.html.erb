<div class="container">
<div class="col-3 mt-5 mb-5 secondary text-center">
<h2>注文履歴詳細</h2>
</div>
</div>

<div class="container">
<div class="row mx-auto">
    

<div class="filed row">
<label for ="customer_name" class="col-3">購入者</label>
<div class="col-9">
<%= @customer.full_name %>
</div>
</div>

<div class="filed row">
<label for ="customer_name" class="col-3">注文日</label>
<div class="col-9">
<%= @order.created_at %>
</div>
</div>

<div class="filed row">
<label for ="customer_name" class="col-3">配送先</label>
<div class="col-9">
<%= @order.postal_code %><%= @order.address %><%= @order.name%>
</div>
</div>

<div class="filed row">
<label for ="customer_name" class="col-3">支払方法</label>
<div class="col-9">
<%= @order.payment_method %>
</div>
</div>

<!--form_with model: @customer, url: admin_order_path(@order.id), method: :patch do |f| %>-->
<!--<div class="form-group row">-->
<!--f.label :status, '注文ステータス', class: "col-3 col-form-label" %>-->
<!--<div class="col-9">-->
<!--f.select :status, options_for_select(Order.status), class: "order-select" %>-->
<!--</div>-->
<!--end %>-->
<!--</div>-->

<table class="table">
    <thead class="thead-light">
        <tr>
            <th>商品名</th>
            <th>単価（税込）</th>
            <th>数量</th>
            <th>小計</th>
            <th>製作ステータス</th>
        </tr>
    </thead>
    <% @order_details.each do |order.detail| %>
    <tbody>
        <tr>
            <td><%= @item.name %></td>
            <td><%= order_detail.price.to_s(:delimited) %></td>
            <td><%= order_detail.amount %></td>
            <!--<td>order_detail</td>-->
            <!--if order_detail-->
            <!--<td>販売中</td>-->
            <!--else-->
            <!--<td>販売停止中</td>-->
            <!--end-->
        </tr>
    </tbody>
    <!--end-->
</table>